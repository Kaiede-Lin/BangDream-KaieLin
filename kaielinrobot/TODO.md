# TODO

先按这个清单占位，后面你再告诉我每一项具体要改什么。

---

## 已完成功能

- [x] **吃什么 / 喝什么** — 插件：`koishi/food.js`
  - 指令：`吃什么`、`吃点什么`、`喝什么`、`喝点什么`
  - 从本地图库随机返回图片，按子文件夹分类管理，文件名自动作为标签
  - 待办：整理收录更多图库素材

- [x] **Pixiv 随机图片** — 插件：`koishi/pixiv.js`
  - 指令：`来点 [关键词]`、`检车到来点 [关键词]`、`pixiv [关键词]`
  - 支持 Bang Dream 角色别名智能匹配（读取 fuzzy_search_settings.json）
  - 多页随机检索 + 偏好匹配排序，返回图片 + 作品信息
  - 待办：接入 Pixiv 登录态以解锁 R-18 内容

- [x] **发言排行** — 插件：`koishi/message-rank.js`
  - 指令：`发言排行 [日期]`
  - 群内消息计数，按天统计，生成头像+柱状图可视化排行
  - 支持按日期查询历史数据

- [x] **今日老婆** — 插件：`koishi/jrlp.js`
  - 指令：`今日老婆`
  - 每天随机一位 BanG Dream! 角色，生成卡片样式图片
  - 含头像缓存，同一用户同一天返回相同结果

- [x] **个人快捷指令** — 插件：`koishi/custom-shortcuts.js`
  - 指令：`我说 [关键词] 执行 [指令]`、`我说 [关键词] 回答 [文字]`、`问答列表`、`删除问答 [编号]`
  - 每人独立存储，关键词完全匹配触发

- [x] **消息去重** — 插件：`koishi/dedup.js`
  - 自动去除短时间内重复消息，防止刷屏

---

## 总体约束

- 图片库来源：本地寻找，按不同图库分类管理。
- 图片合成风格：参考 `https://box.northcivet.com/petpet/`，但不局限于这一种风格，后面可以扩展多套模板。
- 部署环境：服务器在日本，不需要代理。
- 18r：开放。
- 推送：所有需要推送的内容都推。
- 返回：所有命令都返回对应结果，不做隐藏分支。

---

## 功能清单

- [ ] **图片合成** `优先级: P1 (高)`
  - 指令：一组"操作 + @对象"的组合指令
  - 逻辑：返回"底图 + 头像"的合成 GIF
  - 主题：围绕操作、互动、整活等玩法
  - 备注：不止一种模板，后续可扩展更多合成玩法

- [ ] **官号通知推送** `优先级: P2 (中)`
  - 目标：检测 BanG Dream 官方 B 站账号动态
  - 输出：使用 B 站自带的图片样式进行合成后推送到群里
  - 备注：所有符合条件的动态都推送

- [ ] **解析视频链接** `优先级: P2 (中)`
  - 目标：解析抖音 / Bilibili 视频链接
  - 输出：视频直链 + 视频文件
  - 备注：所有支持的平台结果都返回，不做省略

---

## 后续你可以直接告诉我

1. 先做哪一项。
2. 每一项具体要支持哪些口令。
3. 本地图库、素材目录、接口地址放在哪。
4. 每个功能要不要和现在现有风格保持一致，还是可以单独设计。

---

## 已知问题

| 问题 | 影响 | 建议 |
|------|------|------|
| 后端服务器优先级偏向日服/国服 | 其他服的查询可能不符合预期 | 评估是否改为用户可配置的服务器偏好 |
| skia-canvas 编译依赖较重 | Docker 首次构建时间长，内存占用高 | 探索预构建镜像或 Alpine 精简方案 |
| NapCat 登录状态偶发丢失 | QQ 掉线需重新扫码 | 研究登录态持久化的更稳定方案 |
| Pixiv 未登录态搜索限制 | 无法检索 R-18 内容，部分 tag 搜索受限 | 接入 Pixiv refresh token 登录 |
| 食物图库素材为空时返回路径提示 | 新部署时无图片可用 | 预设少量默认图片或从网络图源 fallback |
